# krctech — 수자원법령 흐름 이식: 봇 제거 + Gemini/OpenRouter Q&A + 법령 DB 구축

## Context

현재 `krctech`(Next.js 14 + Supabase + R2)는 두 가지 AI 기능을 가진다.
- **봇 Q&A**: Python 폴링 워커(`bot/`) + `bot_questions` 테이블 + `/api/bot/*`가 HuggingFace(임베딩 `BAAI/bge-m3` 1024차원, 생성 `Qwen2.5-7B`)로 **비동기** 답변.
- **문서 채팅**: `/ai` 채팅 탭 → `runRagPipeline`(동일 HF 스택)로 업로드 문서 RAG.

사용자는 참고 프로젝트 `/Users/yun/Documents/#Hermes Backend/수자원법령`의 흐름을 본 사이트에 이식하길 원한다. 그 프로젝트의 핵심 패턴은 **"Gemini = 임베딩(`gemini-embedding-001`, 768차원), OpenRouter = 답변 생성(무료 모델 폴백 체인)"** 이며, 질문 → Gemini 임베딩 → Supabase `match_chunks`(코사인 top 8) → 근거 조문 프롬프트 → OpenRouter 답변 + 출처 구조다 (`web/src/pages/api/ask.ts`는 현재 비스트리밍 완성 + `x-sources` 헤더 반환, 본 사이트는 UX를 위해 스트리밍으로 확장).

**목표 (확정된 결정):**
1. **봇 완전 제거** (디자인·레이아웃은 유지).
2. **전면 통일**: HuggingFace를 제거하고 사이트 전체(문서 채팅 + 신규 법령 Q&A)를 **Gemini 임베딩(768) + OpenRouter 생성** 스택으로 이전. 기존 `document_chunks`는 768차원으로 **재임베딩**.
3. **법령 DB 구축**: `2026.02.02 ...참고서(...完).pdf`와 `2026.02.23 ...참고서(...完).hwpx` **두 원본 모두**에서 법령·조문을 추출 → 원본별 결과를 병합·중복 제거·교차 검증 → 법제처 Open API로 조문 전문 보강 → 청크+Gemini 임베딩으로 신규 법령 코퍼스에 저장 → 의미 검색·Q&A 동작.
4. **UI**: `/ai`의 "봇 Q&A" 탭을 **"법령 Q&A"(동기식 스트리밍)** 로 교체.

**사전 점검 메모(보강 근거):**
- PDF는 205쪽, HWPX는 214쪽으로 페이지 수가 달라 한쪽만 정본으로 보면 누락 위험이 있다.
- `pdftotext -layout` 기준 PDF는 `관련법` 라인 62건, 조문 패턴 약 1,490건, `「...」` 법령명 394건이 잡힌다.
- HWPX는 `rhwp` 텍스트 dump만 쓰면 44k자 수준으로 크게 줄지만, 실제 `Contents/section0.xml`의 `<hp:t>` 직접 추출은 약 550k자, 조문 패턴 약 1,504건이 잡힌다. 따라서 HWPX는 `rhwp` dump가 아니라 raw XML 기반 추출을 기준으로 해야 한다.

---

## 데이터 흐름 (목표)

```
[법령 인제스천 — 1회/배치, 로컬 스크립트]
 PDF(02.02) + HWPX(02.23) → 원본별 텍스트 추출 + 위치 앵커(page/line/paragraph)
   → 법령명·조문 후보 추출(관련법 라인 + 「…」 + 제○조/시행령/시행규칙/기준)
   → 후보 병합·중복 제거·미매칭 검증 큐 생성
   → 법제처 Open API로 조문 전문 fetch(open-law-client)
   → 조문 단위 청크(chunker) → Gemini 임베딩(768, RETRIEVAL_DOCUMENT)
   → law_source_documents / law_references / laws / law_chunks upsert

[법령 Q&A — 런타임]
 질문 → Gemini 임베딩(RETRIEVAL_QUERY) + 키워드/조문번호 파싱
   → hybrid match_law_chunks(top 8~12, 벡터+법령명+조문번호)
   → 근거 조문 프롬프트 → OpenRouter 스트리밍 답변 + 출처/신뢰도

[문서 채팅 — 런타임, 기존 유지하되 스택 교체]
 질문 → Gemini 임베딩 → match_document_chunks(768) → OpenRouter 스트리밍
```

---

## Part A — LLM 스택 교체 (Gemini 임베딩 + OpenRouter 생성)

참고 프로젝트와 동일하게 **외부 SDK 없이 REST `fetch`** 로 호출(신규 npm 의존성 0).

**신규 `src/lib/llm/gemini-embeddings.ts`** — `src/lib/huggingface/embeddings.ts` 대체
- `embedQuery(text)`: `POST generativelanguage.googleapis.com/v1beta/models/gemini-embedding-001:embedContent`, `taskType: "RETRIEVAL_QUERY"`, `outputDimensionality: 768` → `number[]`.
- `embedDocuments(texts[])`: 50개 배치, `taskType: "RETRIEVAL_DOCUMENT"`, 429/타임아웃 시 지수 백오프(참고 `ingestion/embed.py` 재현). 기존 `embedTexts` 시그니처 호환.
- 모델 선택은 **일단 `gemini-embedding-001` 768차원 고정**. 공식 문서 기준 최신은 `gemini-embedding-2`지만, 수자원법령 참고 프로젝트와 같은 임베딩 공간을 쓰기 위해 001을 고정한다. 추후 `gemini-embedding-2`로 바꾸면 `document_chunks`와 `law_chunks`를 전부 재임베딩해야 한다.
- REST 구현 시 실제 응답 길이가 항상 768인지 검증하고, 저장 전 `assertEmbeddingDim(embedding, 768)`을 통과하지 못하면 DB insert를 막는다.
- 코사인 검색을 쓰더라도 768 축소 임베딩은 `l2Normalize()`를 한 번 적용해 스코어 분포를 안정화한다. 단, 기존 수자원법령과 동일 결과 비교가 필요하면 normalization on/off를 `.env` 플래그로 둘 수 있다.
- env: `GEMINI_API_KEY`.

**신규 `src/lib/llm/openrouter.ts`** — `src/lib/huggingface/generation.ts` 대체
- 모델 폴백 체인 + 2-pass 재시도(참고 `ask.ts` 재현):
  ```
  MODELS = ["google/gemma-4-31b-it:free", "meta-llama/llama-3.3-70b-instruct:free",
            "qwen/qwen3-next-80b-a3b-instruct:free", "qwen/qwen3-coder:free",
            "cognitivecomputations/dolphin-mistral-24b-venice-edition:free"]
  ```
- `generateStream(messages, maxTokens)`: `POST openrouter.ai/api/v1/chat/completions`, `stream: true`, `temperature: 0.2`, SSE 파싱 → `AsyncGenerator<string>`. 한 모델이 빈 응답/오류면 다음 모델로 폴백.
- `generateText(messages, maxTokens)`: 스트림 누적(기존 시그니처 호환).
- SSE 파서는 OpenRouter가 보내는 comment line(`: ...`)과 빈 `data:`를 무시해야 한다. 스트림 중간 오류는 사용자에게 "일부 모델 혼잡" 메시지를 보내고 다음 모델을 비스트리밍 재시도로 연결할지 정책을 정한다.
- 무료 모델명은 자주 바뀌므로 `src/lib/llm/openrouter-models.ts` 한 곳에서 관리하고, `scripts/check-openrouter-models.mjs`가 `/api/v1/models`로 모델 존재 여부를 점검하게 한다.
- env: `OPENROUTER_API_KEY`.

**호출부 교체 (시그니처 호환이라 import 경로만 변경):**
- `src/lib/rag/pipeline.ts:1-2` — `@/lib/huggingface/{embeddings,generation}` → `@/lib/llm/{gemini-embeddings,openrouter}`.
- `src/lib/rag/retriever.ts` — RPC 그대로(차원만 768로 바뀜).
- `src/app/api/search/route.ts`, `src/app/api/process/[id]/route.ts` — 임베딩 import 경로 변경.
- `src/app/api/chat/[sessionId]/messages/route.ts:106` — `model_used: 'Qwen/...'` → OpenRouter 모델 라벨.
- `src/lib/rag/prompt-builder.ts` — provider 무관(메시지 배열)이라 변경 불필요.

**제거:** `src/lib/huggingface/` 디렉터리, `package.json`의 `@huggingface/inference`.

---

## Part B — DB 마이그레이션

신규 마이그레이션 파일을 `supabase/migrations/`에 순서대로 추가(기존 파일은 히스토리로 보존).

**`00013_switch_embeddings_to_768.sql`**
- `document_chunks.embedding`을 768차원으로 변경: HNSW 인덱스 DROP → 컬럼 DROP/ADD `extensions.vector(768)`(기존 1024 임베딩은 모델이 달라 무효 → 재임베딩 전제) → HNSW 인덱스 재생성.
- `match_document_chunks`를 `vector(768)` 시그니처로 `CREATE OR REPLACE`(본문 로직 동일, `00012`의 권한·인증 하드닝 유지).

**`00014_create_law_corpus.sql`** — 참고 `db/schema.sql`의 `documents`/`chunks`/`match_chunks` 패턴 이식
- `law_source_documents`: `id`, `source_type`('pdf'|'hwpx'), `filename`, `source_date`, `page_count`, `text_hash`, `extractor_version`, `raw_text_path`, `created_at`. 두 원본의 추출 결과를 재현하기 위한 감사 테이블.
- `law_references`: 기존 `legal_references`와 별도로 전역 법령 후보를 저장하거나, 기존 `legal_references`를 확장한다. 필수 필드: `source_document_id`, `topic_id`, `law_name`, `canonical_law_name`, `article_ref`, `api_target`, `law_id`, `mst`, `source_anchor`(page/line/paragraph), `confidence`, `match_status`('resolved'|'ambiguous'|'not_found'|'external'|'needs_review'), `raw_context`.
- `laws`: `id`, `law_name`, `canonical_law_name`, `law_id`(법령ID), `mst`, `api_target`('law'|'eflaw'|'admrul'|'external'), `doc_type`, `ministry`, `effective_date`, `source`('lawgokr'|'manual'|'source_only'), `raw_text`, `content_hash`, `created_at`, `updated_at`. UNIQUE(`api_target`, `coalesce(law_id,mst,canonical_law_name)`) 성격의 중복 방지 필요(Postgres expression index 권장).
- `law_chunks`: `id`, `law_id`(FK→laws ON DELETE CASCADE), `chunk_index`, `article_no`, `article_title`, `content`, `embedding extensions.vector(768)`, `metadata jsonb`, `created_at`. UNIQUE(`law_id`,`chunk_index`) + HNSW(cosine) 인덱스.
- `law_chunk_sources`: `chunk_id`, `source_document_id`, `reference_id`, `source_anchor`, `matched_text`, `created_at`. 답변 출처가 법제처 조문뿐 아니라 "참고서 어느 부분에서 추출됐는지"까지 역추적되게 한다.
- `law_aliases`: `alias`, `canonical_law_name`, `api_target`, `law_id`, `mst`, `confidence`, `verified_at`. 예: `건설기술진흥법` ↔ `건설기술 진흥법`, `국토계획법` ↔ `국토의 계획 및 이용에 관한 법률`.
- `law_match_failures`: `law_name`, `article_ref`, `api_target`, `source_anchor`, `reason`, `candidate_hits jsonb`, `created_at`. "모조리 추출" 목표에서 가장 중요한 검수 큐.
- `law_ingestion_runs`: 감사 로그(선택, 참고 `ingestion_runs` 동일).
- `match_law_chunks(query_embedding vector(768), match_count int = 8, query_text text = null, law_name_filter text = null, article_filter text = null)` RPC: `1 - (embedding <=> q)` 코사인 + 법령명 trigram/tsvector + 조문번호 exact boost를 합산해 조문·법령명·출처·source_anchor·similarity를 반환. `authenticated`/`service_role` GRANT.
- RLS: `authenticated` SELECT 읽기 전용, 쓰기는 service_role.

**`00015_drop_bot_questions.sql`**
- `DROP TABLE IF EXISTS public.bot_questions CASCADE;` (봇 제거).

---

## Part C — 법령 인제스천 (PDF+HWPX → 추출 → 전문 보강 → 임베딩 → 저장)

법제처 API 호출 + 임베딩이 수백 건이라 서버리스 타임아웃을 피해 **로컬 Node 스크립트**로 1회 배치 실행(참고 프로젝트가 인제스천을 GitHub Actions로 분리한 것과 동일 사상). 작은 재동기화는 admin 엔드포인트로 보완.

**신규 `src/lib/law/source-extractors.ts`**
- `extractPdfSource(buffer)`: 기존 `pdf-extractor.ts`의 TOC/topic 추출을 유지하되, 원문 전체에서 `관련법 : ...`, `「...」`, `<법/시행령/시행규칙/기준 제○조>`, `법 제○조`, `시행령 제○조`, `고시/기준/지침 제○조` 패턴을 모두 후보로 뽑는다. page/line anchor를 보존한다.
- `extractHwpxSource(buffer)`: `parseHwpx`를 쓰되, 구현은 `Contents/section*.xml` raw XML의 `<hp:t>`를 직접 읽어 단락/표 경계를 최대한 보존한다. `rhwp_cli dump-text`류의 레이아웃 API는 표/본문 일부가 빠질 수 있으므로 검증용 보조로만 사용한다.
- 공통 `extractLawCandidates(text, anchors)`로 PDF/HWPX 결과를 같은 타입(`LawCandidate`)으로 정규화한다.
- 공통 정규식·분류 헬퍼는 `pdf-extractor.ts`에서 분리해 `src/lib/law/reference-extractor.ts`로 둔다.

**신규 `scripts/ingest-laws.mjs`** (로컬 실행: `node scripts/ingest-laws.mjs --pdf "<02.02.pdf>" --hwpx "<02.23.hwpx>"`)
1. PDF/HWPX 각각 추출 → `law_source_documents`에 `text_hash`, page_count, extractor_version 기록.
2. `LawCandidate` 병합: `canonical_law_name + article_ref + api_target` 기준 dedupe. 같은 후보가 두 원본에 모두 있으면 confidence 가산, 한쪽에만 있으면 `source_only` 플래그로 남긴다.
3. 후보별 법령명 해석:
   - `law`/`eflaw`: `searchLaw(query, display=10)` → 정확명/약칭/trigram 점수로 1순위 선택.
   - `admrul`: 현재 `getAdminRule(id)`는 ID가 필요하므로 검색 API가 없으면 `law_match_failures`로 보내고, 관리자 검증 후 ID를 채우는 흐름을 둔다.
   - `external`: 법제처에 없는 표준품셈/내부 기준/사이트 명칭은 `laws.source='source_only'`로 원문 청크만 저장하고 답변 시 "법제처 전문 미확인" 배지를 붙인다.
4. 각 법령에 대해 `getLaw({lawId, articleRef})` 또는 `getLaw({mst, articleRef})`로 조문 전문 fetch. 조문 fetch가 빈 배열이면 whole-law fetch 후 로컬 article matcher로 재시도한다.
5. 조문 본문을 `chunker.ts`의 `chunkText`로 청크. 법령 조문은 조 단위 우선, 별표/서식은 별도 `metadata.kind='appendix'`로 분리한다.
6. `gemini-embeddings.embedDocuments`로 768 임베딩(50 배치, dimension assertion, retry/backoff).
7. `laws`/`law_chunks`/`law_chunk_sources` upsert(service role). `content_hash`가 같으면 재임베딩 생략.
8. `law_match_failures`와 `law_ingestion_runs`에 추출 수, 해결 수, 중복 수, 미매칭 수, 임베딩 수, 실패 사유를 기록.
- 호출 한도 보호: 법제처/Gemini 사이 백오프, `--limit`, `--dry-run`, `--resume-run-id`, `--force`, `--only-failures` 플래그.
- 성공 기준: `관련법` 라인 추출 수가 PDF/HWPX 각각 60건 이상, 조문 패턴 추출 수가 1,400건 이상, `match_status='not_found'`가 전체 후보의 5% 이하일 때만 "완료"로 본다.

**(선택) `POST /api/legal/admin/ingest-source`** — admin 업로드형 소규모 재동기화. 대량은 스크립트 권장(타임아웃). `vercel.json`에 maxDuration 300s 추가 시에만.

---

## Part D — 법령 Q&A 엔드포인트 + UI (봇 대체)

**신규 `POST /api/legal/ask/route.ts`** (참고 `ask.ts` 이식)
1. `embedQuery(question)` (Gemini 768).
2. 질문에서 법령명/조문번호/업무항목 키워드 추출(`parseLegalQuery`): 예) `환경영향평가법 제9조`, `지표조사`, `하천수 사용허가`.
3. `supabase.rpc('match_law_chunks', { query_embedding, query_text, law_name_filter, article_filter, match_count: 12 })`.
4. 결과가 약하면 fallback:
   - 법령명 exact/trigram 검색
   - 조문번호 exact 검색
   - 기존 `legal_references`/`law_api_cache` 실시간 조회
5. 근거 조문 컨텍스트 + 시스템 프롬프트("아래 '근거 조문'에 담긴 내용만 사용해 한국 토목 조사설계 관계법령을 안내…", 인용 강제 — 참고 `ask.ts:27-30` 톤).
6. `openrouter.generateStream`로 SSE 스트리밍 + 출처(법령명·조문·법제처 링크·참고서 원본 anchor).
- 인증: 로그인 사용자(`createClient().auth.getUser`). `vercel.json`에 maxDuration 등록.
- 답변 저장이 필요하면 봇 테이블을 되살리지 말고 `chat_sessions`/`chat_messages`에 `mode='law'` 또는 metadata로 보관한다.

**신규 `src/components/ai/LawQAPanel.tsx`** — `BotQuestionsPanel.tsx` 대체
- 동기식 스트리밍 채팅 UI(기존 `chat/[sessionId]` SSE 처리 패턴 재사용). 답변 + 출처 카드(법령명/조문/법제처 링크/참고서 원본).
- 빈 검색 결과, 낮은 신뢰도, OpenRouter 모델 혼잡, Gemini quota 오류 상태를 분리 표시한다.

**`src/app/(dashboard)/ai/page.tsx`**
- 탭 라벨 `'봇 Q&A'` → `'법령 Q&A'`, `BotQuestionsPanel` → `LawQAPanel`. 탭 구조·디자인 유지.

**`src/app/(dashboard)/legal/search/*`** (선택 강화)
- `/api/legal/search`에 `match_law_chunks` 의미 검색 결과 섹션 추가 → "질문에 따른 법령검색" 품질 향상.
- 검색 결과는 `법령명 exact > 조문번호 exact > 하이브리드 점수 > 벡터 점수` 순으로 정렬한다. 사용자가 조문번호를 입력했는데 다른 조문이 먼저 나오면 검색 품질 실패로 본다.

---

## Part E — 기존 문서 채팅/임베딩 재임베딩

차원 변경(1024→768)으로 기존 `document_chunks.embedding`은 모두 무효.
- 운영 절차: 00013 적용 후 각 `documents`를 `POST /api/process/[id]`로 재처리(파싱→청크→**Gemini 768 임베딩**→재삽입). 문서 수가 많으면 admin 일괄 재임베딩 루프 스크립트 추가(`scripts/reembed-documents.mjs`, 청크 content를 그대로 768 재임베딩 → `document_chunks.embedding` 업데이트, 재파싱 불필요).
- `runRagPipeline`은 Part A 교체로 자동 768 사용.

---

## Part F — 봇 제거 / 환경변수 / 의존성

**삭제할 파일/디렉터리:**
- `bot/`(Python 워커 전체)
- `src/lib/bot/`(`answer.ts`, `auth.ts`)
- `src/app/api/bot/`(`ask/route.ts`, `worker/route.ts`)
- `src/components/ai/BotQuestionsPanel.tsx`, `src/components/ai/QuickQuestion.tsx`
- `src/app/(dashboard)/ask/page.tsx`(봇 리다이렉트)
- `src/lib/huggingface/`(Part A)

**수정:**
- `src/types/database.types.ts` — `BotQuestion`, `BotQuestionStatus` 제거.
- `src/app/(dashboard)/dashboard/page.tsx` — `bot_questions` 쿼리 + `QuickQuestion` 렌더 제거(또는 최근 법령 Q&A로 대체).
- `vercel.json` — `api/bot/worker` 항목 제거, `api/legal/ask`(및 인제스천 엔드포인트 사용 시) 추가.
- `package.json` — `@huggingface/inference` 제거.
- `.env.local.example` — `HF_TOKEN`, `WORKER_SECRET` 제거; `GEMINI_API_KEY`, `OPENROUTER_API_KEY` 추가(`LAW_OPEN_API_OC` 유지).

---

## Part G — 검색 품질·운영 보강

**품질 평가 세트**
- `fixtures/law-qa-eval.json` 생성: 최소 30문항.
- 포함 문항 예시: `지표조사 대상과 근거 조문은?`, `전략환경영향평가 대상계획은?`, `생태계보전부담금 산정 기준은?`, `하천점용허가 신청 근거는?`, `농지전용허가와 농지보전부담금 근거는?`, `환경영향평가법 제9조 내용은?`.
- 각 문항은 기대 법령명, 기대 조문, 허용 키워드, 금지 답변(근거 없는 추정)을 가진다.
- `scripts/eval-law-qa.mjs`가 검색 top-k hit rate, 조문 exact hit, 답변 출처 포함률을 측정한다.

**운영 대시보드/로그**
- `law_ingestion_runs`에 `source_pdf_candidates`, `source_hwpx_candidates`, `resolved_count`, `failure_count`, `embedded_count`, `duration_ms`, `model`, `embedding_dim` 저장.
- API 로그에는 `retrieval_count`, `top_similarity`, `model_used`, `fallback_count`, `generation_id`(OpenRouter 응답 header), `latency_ms`를 남긴다.
- `scripts/check-llm-env.mjs`: Gemini embed 1회, OpenRouter non-stream 1회, stream 1회, Supabase RPC 1회를 실행해 배포 전 키/모델/RPC 문제를 빠르게 잡는다.

**보안/권한**
- `OPENROUTER_API_KEY`, `GEMINI_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `LAW_OPEN_API_OC`는 서버 전용으로만 사용한다. 클라이언트 번들에 노출되지 않도록 `NEXT_PUBLIC_` 접두사를 쓰지 않는다.
- `match_law_chunks`는 읽기 전용 RPC지만 `SECURITY DEFINER` 사용 시 `SET search_path = public, extensions`를 명시한다.
- 법령 Q&A는 로그인 사용자만 허용하되, 법령 코퍼스 자체는 사용자별 데이터가 아니므로 RLS는 authenticated read-only로 단순화한다.

**모델 변경 정책**
- Gemini 임베딩 모델명/차원 변경은 DB 전체 재임베딩이 필요한 breaking change로 본다.
- OpenRouter 무료 모델 체인은 코드 상수 + 헬스체크 스크립트로 관리하고, 특정 모델 장애는 전체 서비스 장애로 취급하지 않는다.
- OpenRouter가 자체 provider fallback을 제공하더라도 무료 모델의 빈 응답/429/5xx는 애플리케이션 레벨에서 한 번 더 감싼다.

**외부 API 확인 메모(2026-06-18 기준)**
- Gemini 공식 문서 기준 최신 임베딩 모델은 `gemini-embedding-2`이고, 텍스트 전용으로 `gemini-embedding-001`도 계속 제공된다. 본 계획은 참고 프로젝트와의 호환성을 위해 001/768차원을 고정한다. 참고: https://ai.google.dev/gemini-api/docs/embeddings
- Gemini 문서에는 `gemini-embedding-001`과 `gemini-embedding-2`의 다중 입력 처리 방식 차이가 명시되어 있다. 001에서 2로 바꾸는 것은 단순 모델명 교체가 아니라 인제스천/배치/재임베딩 계획 변경이다.
- OpenRouter 공식 문서 기준 Chat Completions는 `stream: true` SSE를 지원하고, SSE comment payload는 무시해야 한다. 참고: https://openrouter.ai/docs/api/reference/streaming
- OpenRouter 모델 목록은 `GET https://openrouter.ai/api/v1/models`로 조회 가능하므로 무료 모델 체인은 배포 전 자동 검증한다. 참고: https://openrouter.ai/docs/api/api-reference/models/get-models

---

## 수정/생성 대상 핵심 파일 요약

| 구분 | 경로 |
|---|---|
| 신규 LLM | `src/lib/llm/gemini-embeddings.ts`, `src/lib/llm/openrouter.ts` |
| 신규 법령 | `src/lib/law/source-extractors.ts`, `src/lib/law/reference-extractor.ts`, `src/app/api/legal/ask/route.ts`, `src/components/ai/LawQAPanel.tsx` |
| 신규 스크립트 | `scripts/ingest-laws.mjs`, `scripts/reembed-documents.mjs`, `scripts/eval-law-qa.mjs`, `scripts/check-llm-env.mjs`, `scripts/check-openrouter-models.mjs` |
| 신규 마이그레이션 | `00013_switch_embeddings_to_768.sql`, `00014_create_law_corpus.sql`, `00015_drop_bot_questions.sql` |
| 수정 | `src/lib/rag/pipeline.ts`, `src/app/api/search/route.ts`, `src/app/api/process/[id]/route.ts`, `src/app/api/chat/[sessionId]/messages/route.ts`, `src/app/(dashboard)/ai/page.tsx`, `src/app/(dashboard)/dashboard/page.tsx`, `src/types/database.types.ts`, `vercel.json`, `package.json`, `.env.local.example` |
| 재사용 | `open-law-client.ts`(searchLaw/getLaw/getAdminRule), `cache.ts`, `chunker.ts`, `name-canonicalizer.ts`, `article-code.ts`, `pdf-extractor.ts` 헬퍼 |
| 삭제 | `bot/`, `src/lib/bot/`, `src/app/api/bot/`, `src/lib/huggingface/`, `BotQuestionsPanel.tsx`, `QuickQuestion.tsx`, `(dashboard)/ask/page.tsx` |

---

## 검증 방법

1. **빌드/린트**: `npm run build`, `npm run lint` 통과.
2. **DB 마이그레이션**: Supabase에 00013~00015 적용 → `document_chunks.embedding`/`law_chunks.embedding`이 `vector(768)`, `match_law_chunks` 생성, `bot_questions` 삭제 확인.
3. **원본 추출 스모크 테스트**:
   - PDF: `node scripts/ingest-laws.mjs --pdf "...02.02.pdf" --dry-run` → `관련법` 라인 60건 이상, 조문 패턴 1,400건 이상.
   - HWPX: `node scripts/ingest-laws.mjs --hwpx "...02.23.hwpx" --dry-run` → `Contents/section*.xml` 기반 추출로 `관련법` 라인 60건 이상, 조문 패턴 1,400건 이상.
4. **법령 인제스천**: `node scripts/ingest-laws.mjs --pdf "...02.02.pdf" --hwpx "...02.23.hwpx"` → `law_ingestion_runs`에서 `failure_count / candidate_count <= 0.05`, `embedded_count > 0`, `law_chunks.embedding IS NOT NULL` 확인.
5. **미매칭 검수**: `law_match_failures`를 확인해 행정규칙 ID 미확인, 약칭 매칭 실패, external 분류가 의도된 것인지 검토. `needs_review`가 남아 있으면 운영 완료로 보지 않는다.
6. **법령 Q&A**: `/ai` "법령 Q&A" 탭에서 예) "지표조사 대상과 근거 조문은?" 질문 → 스트리밍 답변 + 정확한 법령·조문 출처 노출. `curl -N POST /api/legal/ask`로 SSE 확인.
7. **법령 검색**: `/legal/search?q=환경영향평가법 제9조` → 해당 법령명/조문이 최상위. `/legal/search?q=환경영향평가` → 관련 조문이 상위 노출.
8. **평가 세트**: `node scripts/eval-law-qa.mjs` → top-5 조문 hit rate 90% 이상, 답변 출처 포함률 100%, 근거 없음 질문의 hallucination 0건.
9. **문서 채팅(회귀)**: 문서 재임베딩 후 `/ai` 채팅 탭에서 업로드 문서 질의 → OpenRouter 답변 + 출처 정상.
10. **봇 제거 확인**: `/api/bot/*` 404, `/ask` 경로·봇 탭·`QuickQuestion`·`HF_TOKEN` 의존 부재.
11. **폴백 동작**: OpenRouter 1순위 모델 강제 실패(잘못된 키/모델) 시 다음 모델로 폴백되어 답변 생성되는지 로그 확인.
12. **헬스체크**: `node scripts/check-llm-env.mjs`와 `node scripts/check-openrouter-models.mjs` 통과.

---

## 미해결/주의

- **두 원본 병합 기준**: PDF(02.02)와 HWPX(02.23)가 서로 다르면 02.23 HWPX를 최신 업무 참고서로 우선하되, PDF에만 있는 법령/조문은 삭제하지 말고 `source_only_pdf`로 남긴다. 실제 업무 기준일을 명확히 표시해야 한다.
- **HWPX 추출 충실도**: 현 `hwpx-parser`의 `<hp:t>` 직접 추출은 쓸 수 있지만, 단락/표/셀 순서가 완벽하지 않을 수 있다. 조문 일부가 분리되면 `<hp:p>`, `<hp:tbl>`, `<hp:tc>` 경계를 보존하는 extractor로 보강한다.
- **PDF 레이아웃 노이즈**: PDF는 줄바꿈과 공백이 법령명 중간에 들어간다. `normalizeLawName`에서 줄바꿈/띄어쓰기/중점/괄호 약칭을 강하게 정규화해야 한다.
- **법제처 매칭 신뢰도**: 약칭→정식명 해석 실패분은 `law_aliases` 또는 `name-canonicalizer.ABBREV_MAP` 보강. 자동 선택 confidence가 낮으면 반드시 `law_match_failures`로 보내고 답변 corpus에 넣지 않는다.
- **행정규칙/고시/기준**: 법제처 행정규칙은 ID 기반 조회가 필요할 수 있다. 자동 검색이 불안정하면 관리자 검증 큐를 먼저 완성해야 한다.
- **별표/서식 검색**: 비용 산정 기준은 별표가 핵심인 경우가 많다. 별표를 조문 청크에 섞지 말고 `metadata.kind='appendix'`로 태깅해 검색 결과에서 별도로 보여준다.
- **OpenRouter 무료 모델**: 한도/가용성 변동 시 모델 체인 갱신 필요. 모델명은 배포 전 헬스체크를 통과해야 한다.
- **Gemini 임베딩 버전**: 공식 문서상 최신 모델은 `gemini-embedding-2`지만, 현재 계획은 수자원법령 흐름과 호환하기 위해 `gemini-embedding-001`을 고정한다. 변경 시 전체 재임베딩 + RPC 차원 검증이 필요하다.
- **법률 답변 안전장치**: 답변은 법률 자문이 아니라 업무 참고용이라는 문구를 UI 하단 또는 답변 disclaimer에 짧게 둔다. 근거 조문이 없으면 추정 답변 금지.
