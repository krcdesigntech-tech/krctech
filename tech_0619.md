# krctech — Hermes 에이전트 기반 자가학습(self-improving) 법령AI 고도화

## Context

현재 krctech 백엔드는 정적인 RAG다: 질문 → HF 임베딩 → `match_law_chunks` → Groq 우선/OpenRouter 폴백 답변. 답변 품질이나 누락 법령을 **스스로 개선하지 못하고**, 질문/답변/피드백이 **전혀 저장되지 않아** 학습 신호가 없다.

사용자는 이 백엔드에 **이 PC에 전역 설치된 Hermes Agent**(`/Users/yun/.local/bin/hermes`, 실체 `~/.hermes/hermes-agent/`)를 "입혀서" **자율 에이전트 루프로 자가학습**하는 시스템으로 고도화하길 원한다.

**확정된 결정**
- 자가학습 = **자율 에이전트 루프** (모델 파인튜닝 X). 사용 신호로 지식·검색·프롬프트를 개선.
- Hermes 에이전트 = **백엔드 코드네임 + 에이전트 계층**, 그리고 **로컬 Hermes Agent CLI를 실제 러너/두뇌로 사용**.
- 자율성 = **혼합**: 저위험(별칭·재랭킹·평가)은 자동, 중요변경(코퍼스 법령 추가·정식명 매핑)은 **관리자 승인**.
- **모델 제약(중요)**: 1단계 실행안은 **로컬 모델(LM Studio)·GPT OAuth(openai-codex) 사용 금지, OpenRouter(생성/추론/판정) + HuggingFace(임베딩)** 조합으로 고정한다. 기존 "Gemini API + OpenRouter API 병행" 요구를 유지하려면 Gemini는 생성 provider 후보로만 별도 분기하고, Gemini 임베딩(768차원)과 HF 임베딩(1024차원)은 혼용하지 않는다.
- 1단계 범위 = 4가지 전부: ①Q&A 로깅+피드백 ②지식 자동 보강 ③별칭·재랭킹 자가개선 ④품질 회귀 평가 자동화.

### Hermes Agent 통합 표면 (조사 결과)
- `hermes -z "<prompt>" --provider openrouter -m <model> --skills <s> -t terminal,file,web --yolo --workdir /Users/yun/Documents/krctech` → 원샷 에이전트(도구호출, 최종 텍스트 stdout). 즉시 실행은 `--skills` 복수 옵션.
- `hermes cron create "<schedule>" "<prompt>" --skill <s> --workdir <proj> --deliver telegram:<id>` → 내장 스케줄러(launchd gateway) 자율 실행 + Telegram 전달. 크론 생성은 `--skill` 단수 옵션을 반복 사용. 잡: `~/.hermes/cron/jobs.json`, 로그: `~/.hermes/cron/output/`.
- 커스텀 스킬: `~/.hermes/skills/<name>/SKILL.md`(frontmatter) + `scripts/`(자동 도구화).
- 현재 Hermes는 OpenRouter 미설정(✗) 상태 → **OpenRouter 키 등록 + provider=openrouter 설정 필요**(Part 4).

### 타당성 검토 결론 (2026-06-19)
- 방향은 타당하다. 이 계획의 "자가학습"은 모델 파인튜닝이 아니라 **운영 로그 → 실패 유형 분석 → 별칭/평가셋/프롬프트/코퍼스 제안 개선** 루프이므로 현재 Supabase RAG 구조와 잘 맞는다.
- 다만 즉시 구현 전 **선행 정합성 보강**이 필요하다. 현재 코드는 HF `bge-m3` 1024차원으로 돌아가지만 일부 스크립트 주석/기록값에 과거 Gemini 768차원 흔적이 남아 있고, 온라인 답변 생성은 Groq 우선이다.
- 모델 요구사항은 다시 확인해야 한다. 이 문서는 Gemini 임베딩 장애/504 이후의 HF 전환을 전제로 하므로, "Gemini API 병행"을 여전히 원하면 **Gemini 생성 fallback**인지 **Gemini 임베딩 복귀**인지 먼저 결정해야 한다.
- Hermes를 "두뇌"로 쓰는 것은 가능하지만, `--yolo` 셸 권한과 서비스롤 키가 결합되면 위험하다. Hermes가 직접 DB를 자유롭게 조작하지 못하게 하고, 프로젝트가 제공하는 제한된 npm 스크립트만 실행하게 해야 한다.
- Q&A 로그는 품질 개선의 핵심 신호지만 개인정보와 민감 질의가 들어올 수 있다. 저장 필드, 보존 기간, 관리자 조회 권한, 원문 마스킹 정책을 먼저 정해야 한다.
- 누락 법령 자동 적재는 가장 위험한 단계다. 에이전트는 `agent_proposals`에 근거와 제안만 남기고, 실제 `laws`/`law_chunks` 변경은 관리자 승인 후 idempotent 스크립트로 처리해야 한다.

---

## 아키텍처 — 2개 평면 (OpenRouter + HF 전용)

```
[온라인: Vercel + Supabase] 신호 수집 + 저위험 자동개선
  /api/legal/ask  → qa_logs(질문·필터·검색결과·답변·모델·지연) 기록
  LawQAPanel      → 👍/👎/근거부족 → /api/legal/feedback → qa_feedback
  query-parser/ask→ law_aliases(활성화)로 법령명 확장 + 피드백 기반 재랭킹

[오프라인: 로컬 Hermes Agent — 두뇌=OpenRouter] 자율 루프(cron, 혼합 자율)
  hermes cron(야간, provider=openrouter) → skill: krctech-self-learning →
    1) collect-signals  : qa_logs/feedback/law_match_failures/저점수 질문 수집(JSON, 읽기전용)
    2) LLM 판단(OpenRouter): 누락·오매칭 법령 진단, 답변 groundedness 판정
    3) 저위험 자동 적용  : 별칭 학습(law_aliases), eval 갱신, 재랭킹 가중치
    4) 중요변경 제안     : agent_proposals(add_law/recanonicalize) — status=pending
    5) eval 회귀 측정    : eval_runs 기록 + 회귀 시 경고
    6) Telegram 요약 전달
  관리자 승인(/admin/legal) → apply-proposal → 기존 ingest 로직(법제처+HF)으로 코퍼스 반영
```

**모델 사용 원칙**: 임베딩=HF(`bge-m3`, 1024차원), 에이전트/추론/판정=OpenRouter. **Groq·로컬·GPT-OAuth 미사용**은 우선 에이전트/판정 경로에 강제한다. 온라인 Q&A까지 OpenRouter 단독으로 바꾸려면 `generateText`의 Groq 우선 체인을 제거하는 별도 변경이 필요하다(한도/비용 영향 있음).

**안전 설계(핵심)**: Hermes 루프는 `--yolo`라도 **코퍼스를 직접 변경하지 않는다**. 고위험 변경은 `agent_proposals`에 *쓰기만* 하고, 실제 적용(`apply-proposal`)은 **관리자 승인 후 별도 스텝**에서만 실행 → 혼합 자율성 + 오적재 방지.

---

## Part 0 — 선행 정합성/게이트 (필수)

자가학습 루프 구현 전에 현재 코드와 DB 상태를 먼저 맞춘다. 이 단계가 빠지면 에이전트가 잘못된 차원/모델 메타데이터를 기준으로 개선 제안을 만들 수 있다.

- **임베딩 차원 정합성**: `supabase/migrations/00016_embeddings_back_to_1024_hf.sql` 적용 여부 확인. `law_chunks.embedding`은 1024차원 HF 기준이어야 한다.
- **스크립트 잔여 Gemini 표기 제거**: `scripts/ingest-laws.mjs`, `scripts/check-llm-env.mjs`, `scripts/reembed-documents.mjs`, `scripts/eval-law-qa.mjs`의 주석·로그·run metadata를 HF `BAAI/bge-m3`, 1024차원으로 통일. 특히 `law_ingestion_runs.model='gemini-embedding-001'`, `embedding_dim=768` 기록값은 수정 필요.
- **중복/실수 정리**: `scripts/ingest-laws.mjs`의 중복 `const t0 = Date.now()`와 chunk row의 중복 `embedding` 키를 제거.
- **현행 생성 경로 명시**: 온라인 Q&A는 현재 Groq→OpenRouter 폴백이다. "사이트 전체 OpenRouter 단독"을 목표로 확정하면 `src/lib/llm/openrouter.ts`에서 Groq 체인을 제거하고 OpenRouter 크레딧/429 대응을 함께 설계한다.
- **Gemini 병행 분기**: Gemini를 다시 쓴다면 권장안은 생성 fallback(`Gemini → OpenRouter` 또는 `OpenRouter → Gemini`)으로 제한한다. Gemini 임베딩 복귀는 768차원 migration, 전체 코퍼스 truncate/reingest, 504 재발 대응이 필요하므로 별도 프로젝트로 분리한다.
- **미사용 코드 정리**: HF 고정이 확정되면 `src/lib/llm/gemini-embeddings.ts`는 deprecated 표시 또는 제거 대상이다. 과거 migration(00013/00014)의 Gemini 주석은 이력으로 남겨도 되지만 신규 로그/문서는 HF 기준이어야 한다.
- **환경 변수 게이트**: `HF_TOKEN`, `OPENROUTER_API_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `LAW_OPEN_API_OC` 확인. Hermes 환경(`~/.hermes/.env`)에는 최소 `OPENROUTER_API_KEY`만 두고, 서비스롤 키는 가급적 프로젝트 스크립트 실행 환경에서만 읽게 한다.
- **기준 성능 저장**: 변경 전 `npm run check:llm-env`, `npm run eval:law-qa`, `npm run ingest:laws -- --dry-run` 결과를 남긴다. 이후 자가개선의 회귀 기준선으로 사용한다.

---

## Part 1 — 온라인: Q&A 로깅 + 피드백

**마이그레이션 `00017_qa_logs_feedback.sql`**
- `qa_logs`: `id`, `user_id`, `session_id`, `question`, `question_hash`, `parsed`(jsonb), `sources`(jsonb: [{law_id, law_name, article_no, score, chunk_id}]), `source_snapshot`(jsonb), `top_score`(float), `answer`, `status`(success/error), `error_code`, `embedding_provider`, `embedding_model`, `generation_provider`, `model_used`, `prompt_version`, `prompt_hash`, `retrieval_count`, `latency_ms`, `created_at`. 인덱스(created_at, top_score, status, question_hash).
- `qa_feedback`: `id`, `qa_log_id`(FK), `user_id`, `rating`(check 'up'|'down'|'insufficient'), `note`, `created_at`. UNIQUE(qa_log_id,user_id).
- RLS: 일반 사용자는 본인 행 read/update-feedback만 허용, service write. 관리자는 `/admin/legal` aggregate/read가 가능해야 하므로 admin role 정책 또는 service-backed admin API를 별도 구성.
- 보존 정책: 원문 `question`/`answer`는 90~180일 보존 후 마스킹 또는 삭제, `question_hash`와 통계 필드는 장기 보존. `.env`, 프롬프트 전문, API 키, 토큰 원문은 저장 금지.

**`src/app/api/legal/ask/route.ts`**
- 검색·생성 후 `qa_logs`에 service client로 insert. `done` SSE 이벤트에 `qaLogId` 포함 반환. 재사용: 기존 `parseLegalQuery`, `match_law_chunks`.

**신규 `src/app/api/legal/feedback/route.ts`** — `POST {qaLogId, rating, note}` → `qa_feedback` upsert.

**`src/components/ai/LawQAPanel.tsx`** — 답변 완료 후 👍/👎/"근거 부족" 버튼 + 메모 → `/api/legal/feedback`(done의 `qaLogId` 사용).

**관리자**: `/admin/legal`에 "Q&A 품질" 카드(최근 질문 수, 👎율, 0결과·저점수 비율).

---

## Part 2 — DB: 에이전트 운영 테이블 + 별칭 활성화

**마이그레이션 `00018_agent_loop.sql`**
- `agent_proposals`: `id`, `kind`(check 'add_law'|'add_alias'|'recanonicalize'|'reembed_law'|'note'), `payload`(jsonb), `payload_schema_version`, `rationale`, `evidence`(jsonb: qa_log/failure ids), `risk_level`(low/medium/high), `status`(check 'pending'|'approved'|'rejected'|'applied'|'failed' default 'pending'), `created_by` default 'hermes', `reviewed_by`, `reviewed_at`, `applied_at`, `result`(jsonb), `created_at`. 인덱스(status, kind, created_at).
- `agent_runs`: `id`, `started_at`, `finished_at`, `kind`(collect/improve/eval), `status`(running/success/failed), `provider`, `model`, `prompt_version`, `token_budget`, `signals`(jsonb), `auto_actions`(jsonb), `proposals_created`(int), `summary`, `errors`(jsonb).
- `eval_runs`: `id`, `created_at`, `total`, `law_hit`, `article_hit`, `law_hit_rate`, `article_hit_rate`, `k`, `notes`.
- `agent_locks`: `name`(PK), `locked_at`, `expires_at`, `owner`, `run_id` → 야간 cron 중복 실행 방지.
- RLS: authenticated read, service write.
- `law_aliases`(기존 미사용) 실제 사용 시작(Part 3·6).
- `agent_proposals.payload`는 kind별 zod schema로 검증한다. 예: `add_law`는 `law_name`, 선택 `law_id`, `reason`, `evidence_qa_log_ids`; `add_alias`는 `alias`, `canonical_law_id` 또는 `canonical_law_name`.

---

## Part 3 — 에이전트 액션 스크립트 (`scripts/agent/*.mjs`, tsx)

기존 라이브러리 재사용(상대 import, `.env.local` 자동 로드).

- `collect-signals.mjs`: `qa_logs`(최근 N)·`qa_feedback`(👎/insufficient)·`law_match_failures`(open)·저점수/0결과 질문 → 구조화 JSON. **읽기 전용**.
- `learn-aliases.mjs`(저위험 자동): `law_references` resolved 매핑 + `laws` 약칭을 `law_aliases`에 upsert. 재사용: `name-canonicalizer`.
- `ingest-one-law.mjs`(승인 적용): 법령명/law_id 1건 → `searchLaw`→`getWholeLaw`→`chunkText`→**`hf-embeddings.embedDocuments`**→`laws`/`law_chunks` upsert. 기존 `ingest-laws.mjs` 단건경로 추출 재사용.
- `propose.mjs`: 제안을 `agent_proposals`에 insert(고위험 유일 쓰기 경로).
- `apply-proposal.mjs <id>`: `approved` 제안만 적용 → `applied`/`failed`, `result` 기록. idempotent하게 작성하고 `--dry-run`을 기본 지원.
- `grow-eval.mjs`(저위험 자동): 긍정 피드백 + 출처 명확 + `top_score` 기준 충족 qa_log만 평가셋에 승격. 환각 답변이 평가셋으로 들어가지 않도록 source exact match 검증 필수.
- `run-eval.mjs`: 기존 `eval-law-qa` 로직 함수화 → 평가 후 `eval_runs` 기록.
- **`src/lib/llm/judge.ts`(신규, OpenRouter 전용)**: 에이전트 판정(groundedness/법령 후보 선택)용 LLM 호출을 **OpenRouter만** 쓰는 함수로 분리(기존 `generateText`는 Groq 우선이라 별도). 스크립트가 이 함수 사용.
- 비용/호출 제한: 각 스크립트는 `--limit`, `--since`, `--max-llm-calls`, `--max-tokens` 옵션을 받아 야간 배치 폭주를 막는다.

`package.json`에 `agent:collect`, `agent:improve`, `agent:apply`, `agent:eval` 추가.

---

## Part 4 — Hermes "입히기": OpenRouter 구성 + 스킬 + cron

**Hermes를 OpenRouter로 구성(1회)**
- OpenRouter 키 등록: `~/.hermes/.env`에 `OPENROUTER_API_KEY=...`(프로젝트와 동일 키) 또는 `hermes login openrouter`/`hermes auth add`.
- 기본 provider/model: `hermes model`로 provider=`openrouter`, 모델은 OpenRouter 체인의 강한 모델(예 `meta-llama/llama-3.3-70b-instruct` 또는 크레딧 시 `google/gemini-2.5-flash`). **로컬·openai-codex 프로파일 미사용.**
- 즉시 실행 검증: `hermes -z "OpenRouter 연결 확인만 하고 종료" --provider openrouter -m <model> --workdir /Users/yun/Documents/krctech`
- 크론 전용 profile을 쓰려면 `hermes profile create openrouter --clone` 후 해당 profile의 provider/model을 OpenRouter로 맞춘다. `--profile openrouter`는 profile이 이미 존재할 때만 동작한다.

**Hermes 스킬 `~/.hermes/skills/krctech-self-learning/SKILL.md`**
- frontmatter + 도구 설명. `scripts/`에 프로젝트 위임 래퍼: `collect.sh`/`learn_aliases.sh`/`propose.sh`/`apply_proposal.sh`/`eval.sh` (각 `cd /Users/yun/Documents/krctech && npm run -s agent:*`).
- 혼합 자율 규칙 명시: 저위험은 직접 실행, 코퍼스 변경은 `propose.sh`로 제안만(적용 금지).
- 스킬 도구는 DB를 직접 만지지 않고 npm 스크립트만 호출한다. 서비스롤 키가 필요한 쓰기는 프로젝트 코드의 allowlist된 경로로만 수행.

**Hermes cron 잡(야간 자율 루프) — provider=openrouter**
```
hermes cron create "0 3 * * *" \
  "krctech 자가학습 루프: collect→👎/0결과/저점수·open failures 분석→누락·오매칭 법령은 propose.sh로 제안(적용금지)→별칭 learn_aliases.sh 자동→eval.sh 회귀측정→한국어 요약" \
  --name krctech-self-learning --skill krctech-self-learning \
  --workdir /Users/yun/Documents/krctech --deliver telegram:<chat_id> \
  --profile openrouter
```
- 행동(ingest/eval/embed)은 프로젝트 스크립트가 HF/법제처 키(`.env.local`)로 수행. Hermes는 OpenRouter로 추론·도구호출·전달.
- 현재 `hermes cron create` 옵션에는 `--provider`, `-m`, `--accept-hooks`가 없다. provider/model은 사전에 `hermes model` 또는 OpenRouter 전용 profile로 맞춘 뒤 `--profile`을 지정한다. 크론 생성 전 `hermes cron create --help`로 설치 버전 옵션을 재확인한다.
- 수동 실행은 job name이 아니라 job id 기준이다. `hermes cron list`로 id 확인 후 `hermes cron run <job_id> --accept-hooks`를 사용한다.
- `--yolo`는 즉시 실행 테스트에서만 제한적으로 사용한다. cron은 스킬 래퍼와 프로젝트 스크립트의 권한 제한으로 통제한다.

---

## Part 5 — 관리자 승인 UI (혼합 자율)

- `/admin/legal`에 "에이전트 제안(agent_proposals)" 섹션(기존 `LegalAdminPanel` 패턴 재사용): pending 목록(kind/payload/rationale/evidence) → 승인/거부.
- 신규 `PATCH /api/legal/admin/proposals/[id]`(admin only, `requireAdmin`): 승인 시 `apply-proposal` 트리거(또는 status=approved→다음 틱 적용), 거부 시 rejected.

---

## Part 6 — 별칭 + 재랭킹 자가개선

- **별칭 활용**: `name-canonicalizer`를 확장해 `law_aliases`(DB)도 병합. `query-parser`/`ask`에서 `law_name_filter`를 별칭→정식명 확장.
- **피드백 재랭킹(저위험 자동)**: `ask`에서 `match_law_chunks` 결과를 후처리 — 누적 피드백 신호로 출처 소폭 가중/강등. 단, 법령명 exact match와 조문번호 match는 사용자 피드백보다 우선한다. 효과는 `agent_runs`로 추적.
- **오탐 방지**: 별칭 자동 추가는 빈도, 법령 참조 resolved 매핑, 관리자 승인 여부를 함께 본다. 한 번의 실패 질의만으로 정식 법령명을 바꾸지 않는다.

---

## Part 7 — 품질 회귀 평가 자동화

- `grow-eval.mjs`로 👍 질문을 평가셋에 자동 편입(30+ 목표).
- Hermes cron(주1회 또는 야간 루프 내) `run-eval.mjs` → `eval_runs` 기록. 직전 대비 `law_hit_rate` 하락 시 Telegram 경고 + `agent_proposals`(kind='note').

---

## 수정/생성 핵심 파일

| 구분 | 경로 |
|---|---|
| 마이그레이션 | `supabase/migrations/00017_qa_logs_feedback.sql`, `00018_agent_loop.sql` |
| 온라인 API | `src/app/api/legal/ask/route.ts`(로깅), `src/app/api/legal/feedback/route.ts`(신규), `src/app/api/legal/admin/proposals/[id]/route.ts`(신규) |
| UI | `src/components/ai/LawQAPanel.tsx`(피드백), `src/app/(dashboard)/admin/legal/*`(제안 승인·품질) |
| 라이브러리 | `src/lib/law/name-canonicalizer.ts`(DB 별칭), `src/lib/law/query-parser.ts`(별칭 확장), `src/lib/llm/judge.ts`(신규, OpenRouter 전용 판정) |
| 에이전트 스크립트 | `scripts/agent/{collect-signals,learn-aliases,ingest-one-law,propose,apply-proposal,grow-eval,run-eval}.mjs` |
| Hermes | `~/.hermes/skills/krctech-self-learning/SKILL.md`+`scripts/*.sh`, OpenRouter provider 구성, `hermes cron` 잡 1개 |
| 재사용 | `open-law-client`, `chunker`, `hf-embeddings`, `eval-law-qa`(함수화), `ingest-laws`(단건경로), `requireAdmin`, `match_law_chunks` |

---

## 검증 방법

1. **선행 정합성**: HF 1024차원 migration 적용, 스크립트의 Gemini 768 잔여 표기 제거, `npm run check:llm-env` 통과.
2. **마이그레이션**: 00017/00018 적용 → 신규 테이블·RLS 확인.
3. **로깅·피드백**: `/legal` 질문 → `qa_logs` 행 → 피드백 → `qa_feedback` 기록. 원문 보존/마스킹 정책 확인.
4. **신호 수집**: `npm run agent:collect -- --limit 50` → 저평가/0결과/open failures JSON 출력.
5. **별칭 자동학습**: `npm run agent:improve -- --dry-run` → 후보 확인 → 실제 실행 후 `law_aliases` 채워짐 → "환평법 9조"가 정식명으로 검색.
6. **제안→승인→적용**: 누락 법령 질문 → `agent_proposals`(add_law) 생성 → `/admin/legal` 승인 → `apply-proposal --dry-run` 확인 → 실제 적용 → 재질문 반영.
7. **Hermes 즉시 실행**: `hermes -z "krctech 자가학습 dry-run" --provider openrouter -m <model> --skills krctech-self-learning --workdir /Users/yun/Documents/krctech` → OpenRouter만 사용하는지 확인.
8. **Hermes 크론**: `hermes cron create --help` 옵션 확인 → `--profile openrouter`로 잡 생성 → `hermes cron list`로 job id 확인 → `hermes cron run <job_id> --accept-hooks` → Telegram 요약 + `agent_runs` + cron 로그.
9. **회귀 평가**: `npm run agent:eval` → `eval_runs` hit rate 기록. 기준선 대비 하락 시 note proposal 생성.
10. **안전성**: 에이전트가 `laws`/`law_chunks` 직접 변경 불가, `agent_proposals`만 쓰는지 확인. 중복 cron 실행 시 `agent_locks`로 차단되는지 확인.
11. **모델 경로 검증**: 에이전트/판정 호출이 OpenRouter·HF만 타는지(네트워크/로그) 확인. 온라인 Q&A까지 OpenRouter 단독으로 전환한 경우 Groq 호출 0건 확인.

---

## 미해결/주의

- **OpenRouter 한도**: 무료 모델은 일일 캡이 낮음. 야간 배치라 호출 수는 적지만, 안정성 위해 OpenRouter 소액 크레딧 권장(유료 폴백 모델 사용 가능). HF는 임베딩 전용.
- **기존 Q&A의 Groq**: 현재 답변 생성은 Groq→OpenRouter 폴백. "오직 OpenRouter/HF" 원칙을 기존 Q&A까지 적용하려면 `openrouter-models`에서 Groq 제거(OpenRouter 단독) — 단 OpenRouter 무료 한도로 429 재발 가능(크레딧 필요). **이번 단계 기본은 에이전트/판정만 OpenRouter 강제**, 기존 Q&A Groq 제거 여부는 별도 결정.
- **Gemini 병행 요구**: 최초 요구가 Gemini API + OpenRouter API 병행이라면, 현 문서의 HF/OpenRouter 고정안은 목표 변경이다. Gemini를 생성 fallback으로 둘지, 임베딩까지 Gemini로 되돌릴지 결정해야 하며 후자는 1024차원 HF 코퍼스와 양립하지 않는다.
- **로컬 의존**: 자가학습 루프는 이 PC(Hermes 설치처)에서 실행. PC 꺼지면 루프 미실행(온라인 로깅·피드백은 무관).
- **Hermes OpenRouter 설정 필수**: 현재 미설정 → 키 등록·provider 전환 전엔 에이전트 두뇌 동작 안 함.
- **보안/개인정보**: `--yolo`는 셸 권한 → 스킬 도구를 지정 npm 스크립트로 한정 + 고위험 승인 게이트. `qa_logs` 질문 저장은 RLS 본인 한정, 관리자는 aggregate 중심. `.env`/`auth.json`/`.env.local` 커밋 금지.
- **프롬프트/판정 오염**: 낮은 품질의 답변이나 환각 답변을 평가셋/별칭으로 승격하면 성능이 역으로 악화된다. 승격 조건은 긍정 피드백만이 아니라 출처 exact match, top_score, 관리자 샘플링 검수를 함께 둔다.
- **법령 원천성**: 에이전트가 웹검색으로 찾은 임의 텍스트를 법령 원문으로 저장하면 안 된다. 코퍼스 반영은 법제처 API 또는 검증된 PDF/HWPX 원문 추출 경로만 허용한다.
- **구현 순서 권장**: Part0 → Part1 → 2 → 3 → 6 → 7 → 4 → 5.
